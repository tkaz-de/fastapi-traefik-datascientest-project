apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: dev
  labels:
    app.kubernetes.io/name: fastapi-traefik-datascientest-project
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: platform
    app.kubernetes.io/environment: dev
    app.kubernetes.io/managed-by: github-actions
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: fastapi-traefik-datascientest-project
      app.kubernetes.io/component: backend
      app.kubernetes.io/environment: dev
  template:
    metadata:
      labels:
        app.kubernetes.io/name: fastapi-traefik-datascientest-project
        app.kubernetes.io/component: backend
        app.kubernetes.io/environment: dev
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: backend
          image: ghcr.io/fastapi-liora/fastapi-traefik-datascientest-project-backend:dev
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
          resources:
            limits:
              memory: "512Mi"
              cpu: "500m"
            requests:
              memory: "256Mi"
              cpu: "250m"
          envFrom:
            - configMapRef:
                name: backend-config
            - secretRef:
                name: backend-secret
          env:
            - name: HOST
              value: "0.0.0.0"
            - name: PORT
              value: "8000"
            - name: API_V1_STR
              value: "/api/v1"
            - name: DOCS_URL
              value: "/docs"
            - name: OPENAPI_URL
              value: "/openapi.json"
          readinessProbe:
            httpGet:
              path: /docs
              port: 8000
            initialDelaySeconds: 20
            periodSeconds: 10
            failureThreshold: 5
          livenessProbe:
            httpGet:
              path: /docs
              port: 8000
            initialDelaySeconds: 90
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: dev
  labels:
    app.kubernetes.io/name: fastapi-traefik-datascientest-project
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: platform
    app.kubernetes.io/environment: dev
    app.kubernetes.io/managed-by: github-actions
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: fastapi-traefik-datascientest-project
    app.kubernetes.io/component: backend
    app.kubernetes.io/environment: dev
  ports:
    - port: 8000
      targetPort: 8000
